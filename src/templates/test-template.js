/* eslint-disable arrow-body-style */
/* eslint-disable import/no-extraneous-dependencies */
/* eslint-disable no-unused-vars */
import { html } from 'lit-element';

export const testTemplate = (name) => {
    return html`
        <h1>${name}</h1>
    `;
}
